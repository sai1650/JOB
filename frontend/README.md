Frontend scaffold (Vite + React).

Run:

npm install
npm run dev

This frontend expects the backend API to be available at the same host under `/api` paths. Configure a proxy or run the backend on the same origin for development.